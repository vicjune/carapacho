export const makeUserChoosePlayer = async (
  playerId: string,
  limitations?: { excludePlayerId: string },
): Promise<string | undefined> => {
  return 'fezfvazv';
};

export const makeUserChooseUnit = async (
  playerId: string,
  limitations?: { maximumUnitValue?: number; placeId?: string },
): Promise<string | undefined> => {
  return 'fezfvazv';
};

export const makeUserChoosePlace = async (
  playerId: string,
  limitations?: { excludePlaceId: string },
): Promise<string | undefined> => {
  return 'fezfvazv';
};
